HTML twin of the SVG player circle on the field — use it in sidebars and docs when you need to reference a player.

```jsx
<PlayerToken team="offense" label="QB" />
<PlayerToken team="defense" selected />
<PlayerToken team="offense" label="X" target size={34} />
```

- Offense is always red, defense always blue; labels are ink, never white.
